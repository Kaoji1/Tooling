export const environment = {
  production: false,
  // ปกติถ้า Run Backend ในเครื่องตัวเองจะใช้ localhost
  apiUrl: 'http://localhost:3000/api' 
  
  // 💡 หมายเหตุ: ถ้าคุณต้องการให้ตอนเขียนโค้ด (ng serve) 
  // มันยิงไปหา Server PBGM06 ตลอด ก็ให้แก้บรรทัดบนเป็น:
  // apiUrl: 'http://PBGM06:3000/api'
};