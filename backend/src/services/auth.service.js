// Handles user authentication
login(username, password) → Returns JWT token
logout() → Clears user session
getCurrentUser() → Returns logged-in user info
getUserRole() → Returns user's role (user/purchase)