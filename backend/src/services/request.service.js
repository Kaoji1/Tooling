// Manages purchase requests
createRequest(cartItems) → Creates new request
getUserRequests() → Returns user's request history
getAllRequests() → Returns all requests (for purchase role)
updateRequestStatus(id, status) → Updates request status