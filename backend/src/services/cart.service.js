// Manages shopping cart
addToCart(itemId, quantity) → Adds item to cart
updateCartItem(itemId, quantity) → Updates item quantity
removeFromCart(itemId) → Removes item from cart
getCart() → Returns current cart contents